---
# Install dependencies needed by Ansible for extracting zip archives

- name: install dependencies for ansible to interact with tomcat manager api
  yum:
             pkg={{ item }}
    update_cache=yes
           state=present
  with_items:
    - unzip
