/* Height graph plotting along a linestring, displayed in a popover */

magic.classes.HeightGraphPopup = function(options) {
        
    /* API options */
    
    /* Id of target element for the popover */
    this.target = jQuery(options.target || "height-graph");
    
    
    
};
