/* Embedded Map Creator map region selection dialog */

magic.classes.embedded_creator.MapRegionSelector = function(loadContextCb) {
    
    /* Callback for loading a map context into the wider application */
    this.loadContextCb = loadContextCb;
        
};
